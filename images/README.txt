Place your background image here as **`bg.jpg`**.

The image will be used as the full-screen hero background.  
Recommended: landscape, at least 1920 × 1080 px, JPEG format.

After dropping the file in, the hero section will automatically display it.
